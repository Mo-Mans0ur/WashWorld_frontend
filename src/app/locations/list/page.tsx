// Listesiden for lokationer – viser alle WashWorld-lokationer som en scrollbar liste.
// Al logik og visning håndteres af LocationsList-komponenten.
import LocationsList from "@/components/LocationsList";

export default function Page() {
  return <LocationsList />;
}
